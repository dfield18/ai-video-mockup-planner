[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn ai_video_mockup_planner.api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
