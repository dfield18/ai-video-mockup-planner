[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && uvicorn ai_video_mockup_planner.api:app --host 0.0.0.0 --port $PORT"
